/** Typer som delas mellan server- och klientkomponenter. */

export type NavPost = { href: string; label: string };
export type GalleriBild = { src: string; alt: string };
export type Video = { id: string; titel: string; kalla?: string };
